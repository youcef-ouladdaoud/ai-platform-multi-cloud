apiVersion: v1
kind: Service
metadata:
  name: openclaw
  namespace: ai-platform
  labels:
    app: openclaw
    app.kubernetes.io/name: openclaw
    app.kubernetes.io/component: contract-analysis
spec:
  type: ClusterIP
  selector:
    app: openclaw
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
