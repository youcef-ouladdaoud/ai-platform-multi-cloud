apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ai-platform

resources:
  - ../../base

patchesStrategicMerge:
  - storage-class-patch.yaml

commonLabels:
  app.kubernetes.io/environment: oracle
